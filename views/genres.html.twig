{% extends 'layout.html.twig' %}

{% block content %}
<h2>Genres de film : </h2>

<ul>
{% for genre in genres %}
    <li><a href="{{ path('filmsParGenre',{id: genre.id}) }}">{{ genre.nom }} ({{ genre.nb_films}})</li>
{% endfor %}
</ul>
{% endblock %}
